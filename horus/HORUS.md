# HORUS.md — PuppyPatch Edition

Project instructions for Horus (PuppyPatch Red Team AI Agent).

- Keep diffs small and reviewable.
- Use safe tools only unless approval is explicit.
- Never push, deploy, delete, or edit secrets without approval.
- Prefer TypeScript strictness and tests.
- Run `pnpm build` and `pnpm test` before reporting completion.
- This is PuppyPatch — a Red Team AI Agent for authorized security testing.
