# Phase 1 Boundary Lock

Phase 1 is Core MVP only.

Allowed: TypeScript monorepo, CLI, React Ink TUI, typed event bus, slash commands, prompt input, transcript, right sidebar, mock model, model-router skeleton, file/shell/git tools, checkpoint/diff/undo, safety classifier, config loader, HORUS.md loader, single-agent loop, tests.

Out of scope: swarm, Hermes++ memory/skills, AgentStack++ project generation, gateways, dashboard, cloud backends, scheduling, trajectory export, full MCP.

MCP is placeholder-only until Phase 2.
