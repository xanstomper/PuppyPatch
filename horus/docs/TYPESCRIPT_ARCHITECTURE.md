# Horus TypeScript Architecture

Horus is now TypeScript-first. The primary terminal product is React + Ink, Commander CLI, Zod schemas, typed event bus, SQLite memory, provider-agnostic model routing, MCP-ready adapters, and repo-aware tools.

Python files from the first prototype are legacy/reference only; the product surface is `pnpm horus` / `horus`.
